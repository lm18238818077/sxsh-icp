
const state = () => ({
  icp: null
})

// getters


// actions
const actions = {

}

// mutations
const mutations = {
  init (state, { id }) {
    
  }
}

export default {
  namespaced: true,
  state,
  actions,
  mutations
}